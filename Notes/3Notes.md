# Props
Nothing but Properties

Just like we have attribute and values to div 
<div id='10' className="Card"></div> 
we will have props to components

<Card name='bhu' num={8} arr={}/>

Card function has to recieve
Card(props)
in this props ={name="bhu",age=18}
props.name='bhu'
<p>{props.user}</p>