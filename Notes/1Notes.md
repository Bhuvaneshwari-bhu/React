# What is React?
Javascript Library for building fast and interactive UI and SPA's(single page Application)

Developed by FaceBook (Meta)

React is view layer of an MVC app

Declarative view:(not reload just update)

Component Based Architecture : Encapsulated

# Why React?
Virtual DOM -> state change ->Real DOM
Virtual DOM 
(Js DOM is real DOM , Consumes more memory.)

Fast

npm : node package manager . with this we can install and uninstall packages.

npx : node package execute 

Vite:bundler (instead of create-react-app)


1) $: npm create vite
2) $: cd namaste-world
2) $: npm install
2) $: npm run dev
<!-- 3) $: npm start
        this will start the developmennt server(frontend server)
        https://localhost:3000/ -->
